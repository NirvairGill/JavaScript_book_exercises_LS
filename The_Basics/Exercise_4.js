// Given the code below, identify which lines are statements and which are expressions:

var foo;  // Statement
foo = 5;  // Expression, Anything which evaluate to a value in javaScript is called expression.
foo;      // Expression